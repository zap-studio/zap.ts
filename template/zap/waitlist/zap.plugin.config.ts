import type { WaitlistPluginConfig } from "./zap.plugin.config.types";

export const ZAP_WAITLIST_CONFIG: WaitlistPluginConfig = {
  ENABLE_WAITLIST_PAGE: false,
  TITLE: "try Zap.ts",
  DESCRIPTION: "be the first to build applications as fast as a zap.",
  SHOW_COUNT: true,
};
